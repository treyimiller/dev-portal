import React from 'react';
import ReactDOM from 'react-dom/client';
import LoginPanel from './devcogent-login.tsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LoginPanel />
  </React.StrictMode>
);

